# GitHub Message Thread Previews

Local message thread previews are written here by the Messages tab and the local API.

Each YAML file represents one top-level message as a pull request. Replies are stored as pull-request comment messages. These files are mock artifacts for local development and should not contain secrets.
