# Issue Previews

Mock GitHub issue previews live here during local development.

Each preview records the issue number, title, body, state, labels, assignees, and mock issue URL.
These artifacts let tests and demos verify the GitHub issue adapter boundary without network access.
