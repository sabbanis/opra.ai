# PR Previews

Mock GitHub pull-request previews live here during local development.

Each preview records the generated branch name, commit file list, PR title, PR body, and mock PR URL.
These artifacts let tests and demos verify the GitHub adapter boundary without network access.
