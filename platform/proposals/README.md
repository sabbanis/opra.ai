# Proposals

Reviewable mutation proposals live here during local development.

Current proposal type:

- `mutations`: proposed source-of-truth object changes that have passed schema, domain, and policy checks but have not been applied.

Mutation proposals are local stand-ins for future GitHub pull-request backed change proposals.
