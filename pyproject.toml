[tool.opra]
name = "opra.ai"
status = "free-developer-preview"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
  "packages/company_os_core/src",
  "packages/company_os_cli/src",
]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.mypy]
python_version = "3.9"
strict = true
